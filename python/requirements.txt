numpy>=1.21.0
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.9.0

# PyTorch with MPS (Metal Performance Shaders) support for Apple Silicon
torch>=2.0.0
torchaudio>=2.0.0

# Vocal separation (Apple Silicon optimized)
demucs>=4.0.0

# Pitch extraction (torch-crepe for MPS, fallback to CREPE)
torchcrepe>=0.0.19
crepe>=0.0.12

# Audio features and alignment
dtaidistance>=2.3.10  # Fast DTW implementation

# Utilities
matplotlib>=3.5.0
scikit-learn>=1.1.0
av>=10.0.0  # Video processing (ffmpeg bindings)
tqdm>=4.65.0